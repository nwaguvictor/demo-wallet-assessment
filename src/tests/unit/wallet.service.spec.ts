import { WalletService } from '../../services';

describe('WalletService', () => {
  const walletService = new WalletService();

  it('should load the wallet service module', async () => {
    expect(walletService).toBeDefined();
  });

  describe('fund()', () => {
    it('should load the wallet service module', async () => {
      expect(walletService).toBeDefined();
    });
  });
});
